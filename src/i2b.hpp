#ifndef _I2B_HPP_
#define _I2B_HPP_

#include <fstream>
#include <iostream>
#include <string>

namespace iTob {
  void encode(std::string &outputFile);
  void decode(std::string &outputFile);
}

#endif
